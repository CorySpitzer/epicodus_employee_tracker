describe(List) do
  it("tells which employees are in a division") do
    division1 = Division.create({:name => "division1", :division_id => division.id})
    division2 = Division.create({:name => "division2", :division_id => division.id})
  end
end
